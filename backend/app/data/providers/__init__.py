"""Provider-agnostic market-data interface and implementations.

The rest of the system depends only on MarketDataProvider (base.py).
Implementations live in separate modules (offline.py, tiingo.py in M2, …).
"""
